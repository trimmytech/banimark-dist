<?php

/**
 * BANIMARK CORE - PLACEHOLDER, NOT A BUILD.
 *
 * This file is where the encoded core belongs. It is intentionally empty of
 * product code: the readable sources live only on the vendor's machine, and
 * publishing them here would defeat the point of encoding them.
 *
 * A real release replaces this file wholesale:
 *
 *     IONCUBE_ENCODER=ioncube_encoder8 php build/publish.php
 *
 * Until then any install that reaches this file has no engine, so it stops
 * here with an explanation rather than failing later with a confusing
 * "class not found" from somewhere deep in the host application.
 */

throw new \RuntimeException(
    'Banimark: this build has no encoded core (src/Core.enc.php is a placeholder). '
    .'It is not a working release - obtain a licensed build from your vendor.'
);
